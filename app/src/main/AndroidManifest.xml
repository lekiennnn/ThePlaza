<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_ban_hang"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/app_ban_hang_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doan2"
        tools:targetApi="31">

        <activity
            android:name=".Login.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Login.SignUpActivity"
            android:exported="false" />

        <activity
            android:name=".Login.LoginActivity"
            android:exported="true" />

        <activity
            android:name=".ShoppingCart.PurchaseActivity"
            android:exported="false" />
        <activity
            android:name=".ShoppingCart.ShoppingCartActivity"
            android:exported="false" />
        <activity
            android:name=".shopping_cart"
            android:exported="false" />
        <activity
            android:name=".Event.ItemDetailActivity"
            android:exported="false" />
        <activity
            android:name=".item_details"
            android:exported="false" />

        <activity
            android:name=".Product.MainActivity"
            android:exported="true" />

    </application>

</manifest>
